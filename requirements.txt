# Essential packages for PGU-net project
numpy==2.2.1
matplotlib==3.10.5
scikit-image==0.24.0
scipy==1.13.1
pyparsing==3.1.4

# PyTorch - Use one of the following installation methods:

# Method 1: Install PyTorch with conda (recommended):
# For Python 3.12 + CUDA 11.8:
# conda install pytorch==2.5.1 torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia

# For Python 3.13 + latest CUDA:
# conda install pytorch==2.6.0 torchvision torchaudio -c pytorch

# Method 2: Alternative pip install (if conda not available):
# pip install torch==2.5.1 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# Method 3: CPU-only version:
# pip install torch==2.5.1 torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# Additional packages for visualization and utilities
Pillow>=8.0.0
tqdm>=4.60.0
tensorboard==2.17.0
opencv-python>=4.5.0
pathlib
scikit-learn
seaborn